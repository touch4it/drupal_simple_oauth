name: Simple OAuth (modified)
type: module
description: 'Do NOT update from anywhere except Touch4IT github ! The OAuth 2.0 Authorization Framework: Bearer Token Usage'
version: '8.x-3.5'
core: '8.x'
package: Authentication
configure: oauth2_token.settings
dependencies:
  - drupal:system (>=8.3)
  - serialization
  - consumers
